## Purpose

This is a tiny test PR created by an automated assistant to exercise the
project's Pull Request CI and review flow. The change is intentionally
non-invasive: a new documentation file only.

If this PR is rejected, we'll inspect the CI and address the failures.

Do not include secrets in PRs.
